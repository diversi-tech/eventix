{
  "name": "together",
  "version": "1.0.0",
  "description": "A platform for managing events and coordinating meals together",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"yarn workspace backend start\" \"yarn workspace frontend start\"",
    "build": "yarn workspace frontend build",
    "test": "yarn workspace frontend test",
    "lint": "yarn workspace frontend lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}